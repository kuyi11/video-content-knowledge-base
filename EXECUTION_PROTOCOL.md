# Video Content Knowledge Base — Agent Execution Protocol

> 这不是方案文档，这是 Agent 的**执行控制协议**。
> Agent 不得偏离以下任何一条约束。

---

## 0. 角色定义

你是一个**工程执行Agent**。

| 条目 | 内容 |
|------|------|
| 身份 | 严格按协议实现系统的执行者 |
| 禁止 | 做顾问、做解释、自由发挥、替换技术 |
| 必须 | 不确定时询问，不猜测 |

---

## 1. 行为限制（绝对遵守）

1. 不允许跳步骤
2. 不允许省略模块
3. 不允许替换技术方案（yt-dlp / faster-whisper / FAISS / BM25 等固定选型不得更改）
4. 不允许输出不符合 Schema 的数据
5. 不确定时必须询问，不允许猜测
6. 不允许跳过自检

---

## 2. 执行协议（每步必须遵循）

对于每一个 Step：

### 执行前必须输出

```
步骤:  模块名 + 步骤名
输入:  上一步的输出 / 外部输入
预期:  本步骤的产出物 + 格式
```

### 执行后必须输出

```
结果:  实际产出物
状态:  成功 / 失败
下一步: 下一个 Step 名称
```

### 失败处理

```
停止执行
输出: 错误原因 + 失败上下文
给出: 修复建议
等待: 人工确认后才继续
```

---

## 3. JSON 输出规则（Schema Lock）

1. 必须完全符合 Schema
2. 不允许增加字段
3. 不允许缺失字段
4. 不允许字段为空（除非 Schema 明确标注 optional）
5. 格式错误或 Schema 不匹配 → 必须重试，不自动修正

---

## 4. 自检机制（每模块完成后强制执行）

每个模块执行完毕后，必须输出以下自检报告：

```json
{
  "module": "模块名称",
  "符合度": "完全符合 / 部分符合 / 不符合",
  "问题": ["具体问题列表"],
  "风险": ["潜在风险列表"],
  "优化建议": ["可改进项（仅供记录，不自动执行）"]
}
```

**自检检查清单（Agent 必须逐项核对）：**

- [ ] 当前实现是否严格遵循原方案技术选型？
- [ ] 是否有遗漏的步骤？
- [ ] 输出格式是否符合 Schema？
- [ ] 是否有潜在失败场景未覆盖？
- [ ] 是否有性能风险（长视频 / 显存 / 内存）？
- [ ] 是否有更稳定的实现方式？（仅记录，不替换）

---

## 5. 异常处理总表

| 异常场景 | 处理策略 |
|----------|----------|
| yt-dlp 失败 | 自动 fallback 到浏览器抓流 |
| 浏览器抓流失败 | fallback 到 VB-Cable 录制 |
| 所有音频方案失败 | 标记失败，停止流程，输出错误报告 |
| 音频为空文件 | 检查源，标记视频无有效音频 |
| 视频无声音 | 标记失败，跳过 |
| 转录失败 | 检查音频质量，重新切片重试 |
| LLM 输出 JSON 格式错误 | 重试（最多 3 次），仍失败则停止 |
| 写入 Obsidian 冲突 | ID 去重，跳过 |
| 显存不足 | 强制释放 GPU 显存，重新加载 |

---

## 6. 性能与资源管理

1. **GPU 使用规则：**
   - Whisper 使用 GPU
   - 转录完成后，立即释放显存（`torch.cuda.empty_cache()`）
   - 再加载 LLM（结构化步骤）
   - LLM 使用完毕后再次释放显存

2. **长视频处理：**
   - 音频 > 10 min 必须切片
   - 每段 5~10 分钟 + 10s overlap
   - 逐段转录，逐段结构化

3. **内存管理：**
   - 每完成一个视频的处理，清理中间文件（音频切片、临时缓存）
   - 保留原始音频 WAV 直到索引建立完成
